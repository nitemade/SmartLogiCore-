<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logistics.repository.UserMapper">
    <insert id="insert" parameterType="com.logistics.repository.User">
        INSERT INTO user (username, password, email, phone, role, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectByUsername" parameterType="String" resultType="com.logistics.repository.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectById" parameterType="Long" resultType="com.logistics.repository.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <update id="updateById" parameterType="com.logistics.repository.User">
        UPDATE user
        SET username = #{username}, password = #{password}, email = #{email}, phone = #{phone}, 
            role = #{role}, status = #{status}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>